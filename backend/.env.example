# HuggingFace Token (必要) - 用於 WhisperX 說話者辨識
# 請到 https://huggingface.co/settings/tokens 申請
HF_TOKEN=your_huggingface_token_here

# OpenRouter API Key (必要) - 用於 AI 智能分析
# 請到 https://openrouter.ai 申請
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Redis 設置 (用於任務隊列)
REDIS_URL=redis://localhost:6379/0

# 檔案上傳設置
UPLOAD_DIR=/tmp/audio_uploads
MAX_FILE_SIZE=100  # MB

# WhisperX 設置
DEFAULT_MODEL_SIZE=large-v2
DEVICE=auto  # auto, cpu, cuda
COMPUTE_TYPE=float16  # float16, float32, int8
BATCH_SIZE=16

# 支援的語言
SUPPORTED_LANGUAGES=zh,en,ja,ko
DEFAULT_LANGUAGE=zh

# 日誌設置
LOG_LEVEL=INFO
LOG_DIR=/var/log/whisperx

# Firebase 設置 (可選)
FIREBASE_CREDENTIALS_PATH=/path/to/firebase-credentials.json
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# 安全設置
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com
API_KEY_REQUIRED=false

# 效能設置
MAX_CONCURRENT_JOBS=3
AUTO_CLEANUP_INTERVAL=3600  # 秒
FILE_RETENTION_DAYS=7